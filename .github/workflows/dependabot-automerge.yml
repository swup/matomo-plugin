name: Automerge Dependabot PRs

on:
  pull_request:
    types:
      - opened
      - reopened

permissions:
  pull-requests: write
  contents: write

jobs:
  auto-merge:
    uses: swup/.github/.github/workflows/dependabot-automerge.yml@master
    secrets: inherit
